% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pipeline.R
\name{run_pipeline}
\alias{run_pipeline}
\title{Run rootlength analysis pipeline}
\usage{
run_pipeline(
  file,
  out_dir = "rootlength_outputs",
  mode = "control",
  control_name = "Control (0 g/L)",
  boot = TRUE,
  boot_B = 2000,
  seed = 123,
  device = getOption("rootlength.device", "png"),
  plot_w = getOption("rootlength.plot_w", 9),
  plot_h = getOption("rootlength.plot_h", 5),
  plot_dpi = getOption("rootlength.plot_dpi", 300),
  quiet = TRUE
)
}
\arguments{
\item{file}{Path to the input CSV (strict 5-column schema).}

\item{out_dir}{Output directory to write CSVs/plots/summary (created if missing).}

\item{mode}{"control" (Dunnett vs control) or "explore" (Welch + Games-Howell).}

\item{control_name}{Character. Required if mode = "control".
Use exact label (e.g., "RB (4 g/L)") or pooled treatment (e.g., "RB").}

\item{boot}{Logical. If TRUE and mode = "control", bootstrap GI change\% CIs.}

\item{boot_B}{Integer; bootstrap iterations.}

\item{seed}{Integer; random seed for bootstrap.}

\item{device}{Plot device passed to ggplot2::ggsave ("png","pdf","svg","jpeg","tiff").}

\item{plot_w, plot_h}{Numeric plot width/height.}

\item{plot_dpi}{Numeric DPI for raster devices.}

\item{quiet}{Logical. If TRUE, minimize console messages.}
}
\value{
Invisibly, a list with primary_key, primary_method, results,
games_howell, dunnett_standard, dunnett_hc3.
}
\description{
Reads a strict 5-column CSV:
\enumerate{
\item treatment (character)
\item concentration (numeric)
\item unit (dose unit, e.g., "g/L")
\item root length (numeric)
\item unit (length unit; e.g., "cm","mm","m","in","inch","inches","ft","feet")
}
}
\details{
Converts length units to centimeters, builds groups, and runs either
Dunnett vs user-defined control (with HC3 fallback) or Welch ANOVA + Games-Howell.
Optionally bootstraps GI change \%.
}
